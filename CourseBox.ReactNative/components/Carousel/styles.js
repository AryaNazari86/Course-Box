import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    dotStyle: {
        width: 10,
        height: 10,
        borderRadius: 5,
        marginHorizontal: 8,
    }
});

export default styles;